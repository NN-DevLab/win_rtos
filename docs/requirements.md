# 要件定義

## アプリ名

画像合成アプリ

ビューア機能(ダウンロードしたPNGを表示する)と、合成機能(複数のPNGを組み合わせる)の2つを軸とするアプリ。将来的にはフレームへの当てはめや落書きなど、より高度な合成機能への発展を見据えるが、今回はその第一段階として「2枚を横並びで合成する」までを作る。

## 目的

win_rtos上で、NetX Duo・FileX・GUIXを組み合わせた実用アプリを作り、Eclipse ThreadXエコシステムの実践的な使い方を検証する。

## 制約条件

以下は、方式設計で選び直す対象ではなく、プロジェクトの前提として既に決まっている事項。

- **プラットフォーム**: Windows(Win32シミュレータ)
- **採用RTOS/ライブラリ**: Eclipse ThreadX、NetX Duo、FileX、GUIX

## 機能要件

| # | 内容 | 担当コンポーネント |
|---|---|---|
| FR1 | 画像取得画面で、指定したIPアドレスの機器にFTP接続し、フォルダ・ファイルを一覧表示する | NetX Duo(FTPクライアント)+ Npcapによる実通信 |
| FR2 | フォルダをタッチすると中身を表示し、フォルダを掘ってファイルを探せる | NetX Duo(FTPクライアント) |
| FR3 | ファイルを選択して「取得開始」を押すと、FTPで特定したパスに対してHTTPでダウンロードする | NetX Duo(HTTPクライアント) |
| FR4 | ダウンロードしたPNGをファイルとして保存する。アプリを再起動しても残る | FileX(実ファイルにバックする永続化ドライバ) |
| FR5 | 保存済み画像の一覧(フォルダ)をGUI上に表示する | GUIX |
| FR6 | 一覧からアイテムをタッチすると、そのPNGをランタイムデコードして画面表示する(ビューア機能) | GUIX(`gx_image_reader_png_decode`) |
| FR7 | 一覧画面で「合成」ボタンを押すと、合成モードに切り替わる | GUIX |
| FR8 | 合成モード中に画像を2枚選択すると、横に並べて表示する | GUIX |
| FR9 | 横並び表示中に保存ボタンを押すと、名前入力ダイアログが出て、入力した名前で1枚の画像として保存する。保存後も合成画面に留まる | FileX、GUIX |
| FR10 | 保存時に同名のファイルが既に存在する場合は、エラーを表示する | FileX、GUIX |
| FR11 | ビューア画面で、編集ツールバー(移動など)を使って画像を編集できる。今回実装するのは「移動」のみで、他の編集機能はボタンをグレーアウトで配置するに留める | GUIX |
| FR12 | ビューア画面で編集内容がある場合のみ保存ボタンを有効にする。押すと上書き保存/別名保存を選べ、別名の場合は名前入力ダイアログを経て保存する | FileX、GUIX |
| FR13 | 名前入力時、空欄・使用できない文字・同名重複のいずれかに該当する場合はエラーを表示する(ビューア・合成機能で共通) | FileX、GUIX |

## 技術的裏付け

- ランタイムPNGデコードAPI `gx_image_reader_png_decode(data, size, &pixelmap)` がGUIX標準ライブラリに存在する。ビルド時のGUIX Studioリソース変換に頼らず、任意のバイト列をその場でデコードできる。
- NetX DuoにHTTPクライアントアドオン(`netxduo/addons/http`)、FTPクライアントアドオン(`netxduo/addons/ftp`)がある。
- Npcapはインストール・稼働確認済み。
- PNGエンコード(合成画像の保存用)はGUIXのランタイムライブラリには無いため、libpng(+ zlib)を別途組み込む。詳細は[ADR-0003](decisions/0003-libpng-for-png-encoding.md)を参照。

## 前提条件・未解決事項

- ビルドにはNpcap SDK(`pcap.h` / `wpcap.lib` / `Packet.lib`)が別途必要。手元にあるか未確認。
- 通信相手はWindows PCを想定する。標準機能(IIS)でFTP・HTTPの両方をまかなえるが、初期状態では無効になっており、「Windowsの機能の有効化または無効化」からFTPサイト・既定のWebサイトを有効にしておく必要がある。
- libpng・zlibのソースコードを別途取得し、win32/x86向けにビルドできるようにする必要がある。

## スコープ外

- HTTPS/TLS対応
- 複数枚の一括ダウンロードやアルバム管理などの凝ったUI

## 将来的にやりたいこと(今回は作らない)

- フレームを設定して、複数の画像を当てはめてコラージュを作る
- 画像に落書き(手描き)できる
- 合成する画像の枚数を2枚固定から上限なしにする
